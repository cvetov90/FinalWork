// Задача:

Разработать сайт констуктор ПК. Должен содержать компоненты: каталог товаров, область сборки конфигурации, при добавлении товара в сборку должна проводиться провверка
на совместимость компонентов, перетаскивать товары в область сборки нужно мышкой.

// Логика работы:
1) Информация о комплектующих для ПК хранится в БД/JSON и используется для создания объектов товаров (указания значений свойств объектов) и заполнения корточек товаров
в интерфейсе
2) Есть класс, например, "product", описывающий общие для всех товаров свойства: type, name, description, price, id. Также в этом классе будет описпн метод
issupport, который отвечает за проверку совместимости товаров.
3) Также есть классы, которые описывают каждый из представленных типов комплектующих: мат. плата, ОЗУ, процессор и т.д. Эти классы наследуют свойствва и методы класса "product" и 
добавляют собственные свойства , уникальные для каждого типа комплектующих.
4) На основе информации из БД/JSON создаются объекты классов из п.3
5) Данные о комплектующих в сборке ПК представлены в виде массива уже добавленных объектов.
6) В двумерном массиве/БД/JSON есть информация о проверяемых на совместимость параметрах для каждой пары типов комплектующих.
7) При добавлении товара к сборке (перетаскивании товара из области каталога в область сборки) вызывается метод issupport добавляемого объекта и в цикле перебирает каждый
из уже добавленных к сборке объектов на совместимость с добавляемым. Метод issupport по типам комплектующих для которых определяется совместимость и информации из п.6 находит
свойства объектов которые нужно проверить. Далее проверяется равенство/вхождение в множество свойств добавляемого объекта и свойств объекта уже содержащегося в сборке. Если
равенство/вхождение подтверждено для всех свойств (вернулось true), то метод issupport возвращает true и переходит к проверке следующего объекта в сборке. Если для свех проверок вернулось true, то добавляемый объект попадает в сборку (добавляется в массив объектов сборки). Если при работе issupport найдены несовместимые параметры, то формируется сообщение об ошибке с подсказкой какие именно папрпметры комплектующих несовместимы.
